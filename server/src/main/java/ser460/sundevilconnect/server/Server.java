package ser460.sundevilconnect.server;

public class Server {
    // TODO listen on port
    // for each incoming connection, spin up a new Thread(new ClientHandler(socket)).start()

    public void start() {}
}
